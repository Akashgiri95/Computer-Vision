# Image Acquisition + Face Detection

| Notebook | Description |
|----------|-------------|
| `Image Acquisition Face Detection(Haar).ipynb` | Full pipeline: image capture/loading → preprocessing → Haar Cascade detection → cropping |

**Key learning:** End-to-end computer vision pipeline from raw image to detected face ROI. Preprocessing steps (grayscale, histogram equalisation) and their impact on detection accuracy.
